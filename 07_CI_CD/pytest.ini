[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: deterministic tests of one component
    functional: in-process API behavior tests
    integration: live external-service integration tests
    eval: live LLM quality evaluation tests
